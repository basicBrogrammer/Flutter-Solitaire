import 'package:flutter/material.dart';

class FinishedCards extends StatelessWidget {
  const FinishedCards({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('FinishedCards'),
    );
  }
}
